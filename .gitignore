# Don't check auto-generated stuff into git
coverage
build
node_modules
stats.json

# Cruft
.DS_Store
npm-debug.log
.idea

# Don't check environment-specific stuff as well. Remove .vscode from check-in when being edited.
.env
.chrome
