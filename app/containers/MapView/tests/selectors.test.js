// import { fromJS } from 'immutable';
// import { makeSelectMapViewDomain } from '../selectors';

// const selector = makeSelectMapViewDomain();

describe('makeSelectMapViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
