// import { fromJS } from 'immutable';
// import { makeSelectMainViewDomain } from '../selectors';

// const selector = makeSelectMainViewDomain();

describe('makeSelectMainViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
