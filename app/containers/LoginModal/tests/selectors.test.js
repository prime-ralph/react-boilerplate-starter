// import { fromJS } from 'immutable';
// import { makeSelectLoginModalDomain } from '../selectors';

// const selector = makeSelectLoginModalDomain();

describe('makeSelectLoginModalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
