// import { fromJS } from 'immutable';
// import { makeSelectMapDomain } from '../selectors';

// const selector = makeSelectMapDomain();

describe('makeSelectMapDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
